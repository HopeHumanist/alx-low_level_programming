#include "main.h"
#include <stdlib.h>

/**
 * _abs - ikizo
 *
 * Description: calculates the abs
 *  @h: number to be calculated
 *
 *  Return: 0 (Yondaime)
 */

int _abs(int h)
{
	if (h > 0)
		return (0);
	else
		return (abs(h));
}
