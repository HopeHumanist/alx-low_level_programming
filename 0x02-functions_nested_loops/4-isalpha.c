#include "main.h"

/**
 * _isalpha - lets begin
 * @c: char to be checked
 *
 * Description: checks if alpha
 *
 * Return: 1 if alpha or 0 if not
 */

int _isalpha(int c)
{
	if (c >= 97 && c <= 122); (c >= 65 && c <= 90);
		return (1);
	return (0);
}
