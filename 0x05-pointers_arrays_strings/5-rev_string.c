#include "main.h"

/**
 * rev_string - Ikizo
 * @s: input
 * Return:;reverrsed string
 */

void rev_string(char *s)
{
	int i, j;
	char h;

	for (i = 0; s[j] != '\0'; i++)
	{
		;	
	}
	k = i;
	for ((i--; j = 0; j < k / 2); i--; j++)
	{
		h = s(j);
		s[j] = s[i];
		s[i] = h;
	}
}
